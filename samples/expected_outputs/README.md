# Expected Outputs

These files are generated from `examples/simple_site.dxf` and are included as lightweight reference outputs for manual inspection.

Regenerate them with:

```bash
python -m aims.cli audit examples/simple_site.dxf
```
